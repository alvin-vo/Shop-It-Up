export default interface ProductEntity {
    productId: string
    title: string
    price: number
    category: string
    description: string
    quantity: number
    sellerId: string
    image: string
}

