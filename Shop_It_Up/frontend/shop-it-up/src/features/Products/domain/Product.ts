export default interface Product {
    productId: string
    title: string
    price: number
    category: string
    description: string
    quantity: number
    sellerId: string
    image: string
}
